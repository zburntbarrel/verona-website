import StoryStrip from "@/components/StoryStrip";

export const metadata = {
  title: "Story",
  description:
    "The story and vision behind Verona, from Burnt Banksy and XION to the intelligence layer for AI.",
};

export default function StoryPage() {
  return <StoryStrip />;
}
